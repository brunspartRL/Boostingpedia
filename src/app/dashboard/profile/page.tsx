import Link from "next/link"; import { requireUser } from "@/features/auth/server/auth"; import { SiteHeader } from "@/components/marketing/site-header"; import { Container } from "@/components/layout/container"; import { updateProfileAction } from "./actions";
export const metadata={title:"Profile"}; export const dynamic="force-dynamic";
export default async function ProfilePage({searchParams}:{searchParams:Promise<{saved?:string;error?:string}>}){const identity=await requireUser();const p=await searchParams;return <><SiteHeader/><main className="py-12"><Container className="max-w-3xl"><Link href="/dashboard" className="text-sm text-[var(--muted-foreground)] hover:text-white">← Back to dashboard</Link><h1 className="mt-5 text-4xl font-semibold">Profile</h1><p className="mt-2 text-[var(--muted-foreground)]">Your email is managed by your secure account. Profile fields can be updated here.</p>{p.saved&&<p className="mt-5 rounded-xl border border-emerald-400/20 bg-emerald-400/10 p-3 text-sm text-emerald-200">Profile updated.</p>}{p.error&&<p className="mt-5 rounded-xl border border-rose-400/20 bg-rose-400/10 p-3 text-sm text-rose-200">We could not save your changes.</p>}<form action={updateProfileAction} className="mt-8 space-y-5 rounded-3xl border border-white/10 bg-[var(--surface)] p-6 sm:p-8"><label className="block text-sm font-medium">Email<input value={identity.email} disabled className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-white/[0.03] px-3 text-[var(--muted-foreground)]" /></label><label className="block text-sm font-medium">Full name<input name="fullName" required minLength={2} defaultValue={identity.profile?.full_name??""} className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3" /></label><label className="block text-sm font-medium">Phone<input name="phone" defaultValue={identity.profile?.phone??""} className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3" /></label><label className="block text-sm font-medium">Gamer tag<input name="gamerTag" defaultValue={identity.profile?.gamer_tag??""} className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3" /></label><div className="flex items-center justify-between gap-4"><span className="rounded-full border border-white/10 px-3 py-1 text-xs uppercase text-[var(--muted-foreground)]">Role: {identity.profile?.role??"customer"}</span><button className="rounded-xl bg-[var(--primary)] px-5 py-2.5 text-sm font-semibold">Save profile</button></div></form></Container></main></>}
