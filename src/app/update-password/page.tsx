import { requireUser } from "@/features/auth/server/auth"; import { updatePasswordAction } from "./actions";
export const metadata={title:"Update password"};
export default async function UpdatePasswordPage({searchParams}:{searchParams:Promise<{error?:string}>}){await requireUser();const p=await searchParams;return <main className="grid min-h-screen place-items-center px-4"><div className="w-full max-w-md rounded-3xl border border-white/10 bg-[var(--surface)] p-8"><h1 className="text-3xl font-semibold">Choose a new password</h1>{p.error&&<p className="mt-4 text-sm text-rose-300">Use a password with at least 8 characters.</p>}<form action={updatePasswordAction} className="mt-6 space-y-4"><label className="block text-sm font-medium">New password<input name="password" type="password" minLength={8} maxLength={128} required autoComplete="new-password" className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3" /></label><button className="h-11 w-full rounded-xl bg-[var(--primary)] text-sm font-semibold">Update password</button></form></div></main>}
