import Link from "next/link"; import { forgotPasswordAction } from "./actions";
export const metadata={title:"Reset password"};
export default async function ForgotPasswordPage({searchParams}:{searchParams:Promise<{sent?:string;error?:string}>}){const p=await searchParams;return <main className="grid min-h-screen place-items-center px-4"><div className="w-full max-w-md rounded-3xl border border-white/10 bg-[var(--surface)] p-8"><h1 className="text-3xl font-semibold">Reset your password</h1><p className="mt-2 text-sm text-[var(--muted-foreground)]">We will send a secure recovery link to your email.</p>{p.sent?<p className="mt-5 rounded-xl border border-emerald-400/20 bg-emerald-400/10 p-3 text-sm text-emerald-200">If an account exists for that email, a recovery message has been sent.</p>:<form action={forgotPasswordAction} className="mt-6 space-y-4"><label className="block text-sm font-medium">Email<input name="email" type="email" required className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-black/20 px-3" /></label><button className="h-11 w-full rounded-xl bg-[var(--primary)] text-sm font-semibold">Send recovery link</button></form>}<Link href="/login" className="mt-5 inline-block text-sm text-[var(--muted-foreground)] hover:text-white">Back to sign in</Link></div></main>}
